class Solution {
    static void printSequence(int n) {
        while (n > 0) {
            System.out.print(n + " ");
            n--;
        }
        while (n > 0 || n <= 0) {
            System.out.print(n + " ");
            if (n == 0) break;
        }
    }
}
